# TODO

## Grid幅の改善検証メモ

### 優先: 再センター条件の改善

- [ ] 再センター条件を改善する（乖離率だけでなくボラ/連続足条件を併用）
- [ ] 低リスク設定（`lr2_e` 近傍）で、上記のA/B検証をする

### 次のステップ

- [ ] ボラ連動の動的 `range_pct` を試す（ATRや実現ボラで幅を可変化）
- [ ] トレンド局面フィルタを追加する（強トレンド時はGrid停止/片側のみ）
- [ ] 非対称グリッドを試す（上側/下側で `range` や `levels` を分離）
- [ ] 在庫偏りに応じて幅を調整する（危険側を広げる・発注量を抑える）
