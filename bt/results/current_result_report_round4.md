# 現在結果の振り返りレポート（range_pct=0.01固定）

作成日: 2026-02-19
対象run:

- `results/runs/range_001_only_01/train_top.csv`
- `results/runs/range_001_only_01/oos_eval.csv`

比較元:

- `results/current_result_report_round3.md`

## 結論

`range_pcts` を `[0.01]` 固定にして再評価したが、採用候補は **0/10**（全件 `accepted=False`）。
一方で、前回 (`[0.005, 0.01]`) で見られた極端なDD悪化は抑制され、結果の安定性は改善した。

## OOS評価サマリ（今回）

- accepted: **0/10**
- oos_return_pct: **10/10がマイナス**（`-5743.60` 〜 `-3410.12`、平均 `-4483.69`）
- oos_max_dd_pct: **10/10が閾値8.0超過**（`2104.63` 〜 `2703.97`、平均 `2372.57`）
- oos_pf: **全件1未満**（`0.980731` 〜 `0.981346`、平均 `0.981042`）
- overfit: **10/10がTrue**

## 前回（round3）との比較

round3（`range_pcts=[0.005,0.01]`）:

- oos_return_pct: `-8544.07` 〜 `-3410.12`
- oos_max_dd_pct: `2104.63` 〜 `12044.29`
- oos_pf: `0.980752` 〜 `0.988785`

今回（`range_pcts=[0.01]`）:

- oos_return_pct: **`-5743.60` 〜 `-3410.12`（下限改善）**
- oos_max_dd_pct: **`2104.63` 〜 `2703.97`（大幅安定化）**
- oos_pf: **`0.980731` 〜 `0.981346`（低位で安定）**

## 解釈

- `0.005` を除外したことで、極端DD（1万%超）を生むノイズは除去できた。
- ただし採用判定の本丸である `oos_return >= 0`、`oos_pf >= 1.05`、`overfit=False` は未達。
- 現在のボトルネックは「安定化」ではなく「OOS収益性/PFの底上げ」。

## 次アクション

1. `leverage_values` をさらに保守化（例: `[1.0, 1.5]`）
2. `order_size_ratio_values` をさらに縮小（例: `[0.01, 0.02]`）
3. `range_pcts` は `[0.01]` 維持で再実行し、OOS収益/PFへの寄与を確認
4. それでも全滅なら診断目的で `min_pf: 1.0` を一時適用し、どの条件が最終ボトルネックか切り分け
